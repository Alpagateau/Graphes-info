Si l'on prend une heuristique la fonction unififorme nulle pour l'algoritmh A*, il s'agit alors de Dijkstra. En effet lorsque que l'on choisit le sommet ayant le $d + h$ minimal, il s'agit alors du $d$ minimal exactement comme dans Dijkstra.
